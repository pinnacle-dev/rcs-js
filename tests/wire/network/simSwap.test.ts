// This file was auto-generated by Fern from our API Definition.

import * as Pinnacle from "../../../src/api/index";
import { PinnacleClient } from "../../../src/Client";
import { mockServerPool } from "../../mock-server/MockServerPool";

describe("SimSwap", () => {
    test("check (1)", async () => {
        const server = mockServerPool.createServer();
        const client = new PinnacleClient({ apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { phoneNumber: "+14155550123", periodHours: 240 };
        const rawResponseBody = {
            requestId: "nar_1234567890ABCDEF",
            status: "available",
            simSwapped: false,
            latestSimSwapAt: null,
        };
        server
            .mockEndpoint()
            .post("/network/sim-swap")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(200)
            .jsonBody(rawResponseBody)
            .build();

        const response = await client.network.simSwap.check({
            phoneNumber: "+14155550123",
            periodHours: 240,
        });
        expect(response).toEqual({
            requestId: "nar_1234567890ABCDEF",
            status: "available",
            simSwapped: false,
            latestSimSwapAt: null,
        });
    });

    test("check (2)", async () => {
        const server = mockServerPool.createServer();
        const client = new PinnacleClient({ apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { phoneNumber: "phoneNumber" };
        const rawResponseBody = { key: "value" };
        server
            .mockEndpoint()
            .post("/network/sim-swap")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(400)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.network.simSwap.check({
                phoneNumber: "phoneNumber",
            });
        }).rejects.toThrow(Pinnacle.BadRequestError);
    });

    test("check (3)", async () => {
        const server = mockServerPool.createServer();
        const client = new PinnacleClient({ apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { phoneNumber: "phoneNumber" };
        const rawResponseBody = { error: "error" };
        server
            .mockEndpoint()
            .post("/network/sim-swap")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(401)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.network.simSwap.check({
                phoneNumber: "phoneNumber",
            });
        }).rejects.toThrow(Pinnacle.UnauthorizedError);
    });

    test("check (4)", async () => {
        const server = mockServerPool.createServer();
        const client = new PinnacleClient({ apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { phoneNumber: "phoneNumber" };
        const rawResponseBody = { error: "error" };
        server
            .mockEndpoint()
            .post("/network/sim-swap")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(403)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.network.simSwap.check({
                phoneNumber: "phoneNumber",
            });
        }).rejects.toThrow(Pinnacle.ForbiddenError);
    });

    test("check (5)", async () => {
        const server = mockServerPool.createServer();
        const client = new PinnacleClient({ apiKey: "test", environment: server.baseUrl });
        const rawRequestBody = { phoneNumber: "phoneNumber" };
        const rawResponseBody = { error: "error" };
        server
            .mockEndpoint()
            .post("/network/sim-swap")
            .jsonBody(rawRequestBody)
            .respondWith()
            .statusCode(500)
            .jsonBody(rawResponseBody)
            .build();

        await expect(async () => {
            return await client.network.simSwap.check({
                phoneNumber: "phoneNumber",
            });
        }).rejects.toThrow(Pinnacle.InternalServerError);
    });
});
