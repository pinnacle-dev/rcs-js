// This file was auto-generated by Fern from our API Definition.

import type { BaseClientOptions, BaseRequestOptions } from "../../../../../../BaseClient.js";
import { mergeHeaders, mergeOnlyDefinedHeaders } from "../../../../../../core/headers.js";
import * as core from "../../../../../../core/index.js";
import * as environments from "../../../../../../environments.js";
import * as errors from "../../../../../../errors/index.js";
import * as Pinnacle from "../../../../../index.js";

export declare namespace Test {
    export interface Options extends BaseClientOptions {}

    export interface RequestOptions extends BaseRequestOptions {}
}

export class Test {
    protected readonly _options: Test.Options;

    constructor(_options: Test.Options) {
        this._options = _options;
    }

    /**
     * Create a new RCS test agent for development and testing.
     *
     * ## Overview
     *
     * Test agents let you build and test full RCS functionality — rich cards, carousels, buttons,
     * quick replies, and media messages — without going through the full carrier review process.
     * Messages from test agents can only be sent to [whitelisted phone numbers](/api-reference/rcs-agents/test/whitelist-number).
     *
     * ## Limits
     *
     * - **Maximum 5 test agents per account.**
     *
     * ## Image Requirements
     *
     * | Image | Format | Max Size |
     * |-------|--------|----------|
     * | Logo  | JPEG, PNG | 50 KB |
     * | Hero  | JPEG, PNG | 200 KB |
     *
     * ## After Creation
     *
     * Once your test agent is created, you'll need to:
     *
     * 1. **Whitelist test phone numbers** using [`POST /rcs/test/agents/{agentId}/whitelist`](/api-reference/rcs-agents/test/whitelist-number).
     * 2. **Accept the tester invite** on each whitelisted device.
     * 3. **Send messages** using [`POST /messages/send/rcs`](/api-reference/messages/send-rcs) with the returned agent ID as the `from` field.
     *
     * > **2-Minute Cooldown**
     * >
     * > After creating a test agent, there is a mandatory 2-minute cooldown before you can whitelist phone numbers.
     * > This is a requirement imposed by Google's RBM platform.
     *
     * @param {Pinnacle.rcs.CreateTestAgentRequest} request
     * @param {Test.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Pinnacle.BadRequestError}
     * @throws {@link Pinnacle.UnauthorizedError}
     * @throws {@link Pinnacle.ForbiddenError}
     * @throws {@link Pinnacle.InternalServerError}
     *
     * @example
     *     await client.rcs.test.createAgent({
     *         displayName: "Acme Support",
     *         description: "Get help with your Acme orders and account",
     *         logoUrl: "https://example.com/logo.png",
     *         heroUrl: "https://example.com/hero.png",
     *         phoneNumbers: [{
     *                 number: "+14155550123",
     *                 label: "Support"
     *             }],
     *         emails: [{
     *                 address: "support@example.com",
     *                 label: "Support"
     *             }],
     *         websites: [{
     *                 url: "https://example.com",
     *                 label: "Website"
     *             }],
     *         privacyUrl: "https://example.com/privacy",
     *         termsUrl: "https://example.com/terms",
     *         color: "#FF6B00",
     *         isConversational: true,
     *         agentUseCase: "MULTI_USE"
     *     })
     */
    public createAgent(
        request: Pinnacle.rcs.CreateTestAgentRequest,
        requestOptions?: Test.RequestOptions,
    ): core.HttpResponsePromise<Pinnacle.TestAgentResponse> {
        return core.HttpResponsePromise.fromPromise(this.__createAgent(request, requestOptions));
    }

    private async __createAgent(
        request: Pinnacle.rcs.CreateTestAgentRequest,
        requestOptions?: Test.RequestOptions,
    ): Promise<core.WithRawResponse<Pinnacle.TestAgentResponse>> {
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({ ...(await this._getCustomAuthorizationHeaders()) }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PinnacleEnvironment.Default,
                "rcs/test/agents",
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryParameters: requestOptions?.queryParams,
            requestType: "json",
            body: request,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Pinnacle.TestAgentResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Pinnacle.BadRequestError(_response.error.body as unknown, _response.rawResponse);
                case 401:
                    throw new Pinnacle.UnauthorizedError(
                        _response.error.body as Pinnacle.Error_,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Pinnacle.ForbiddenError(_response.error.body as Pinnacle.Error_, _response.rawResponse);
                case 500:
                    throw new Pinnacle.InternalServerError(
                        _response.error.body as Pinnacle.Error_,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PinnacleError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.PinnacleError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.PinnacleTimeoutError("Timeout exceeded when calling POST /rcs/test/agents.");
            case "unknown":
                throw new errors.PinnacleError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    /**
     * Update an existing RCS test agent's configuration.
     *
     * All fields are optional — only include the fields you want to update.
     *
     * ## Image Requirements
     *
     * If updating images, the same requirements apply as when creating an agent:
     *
     * | Image | Format | Max Size |
     * |-------|--------|----------|
     * | Logo  | JPEG, PNG | 50 KB |
     * | Hero  | JPEG, PNG | 200 KB |
     *
     * > **2-Minute Cooldown**
     * >
     * > After updating a test agent, there is a mandatory 2-minute cooldown before you can whitelist phone numbers.
     * > This is a requirement imposed by Google's RBM platform.
     *
     * @param {string} agentId - The RCS agent ID (must be prefixed with `agent_`).
     * @param {Pinnacle.rcs.UpdateTestAgentRequest} request
     * @param {Test.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Pinnacle.BadRequestError}
     * @throws {@link Pinnacle.UnauthorizedError}
     * @throws {@link Pinnacle.ForbiddenError}
     * @throws {@link Pinnacle.NotFoundError}
     * @throws {@link Pinnacle.InternalServerError}
     *
     * @example
     *     await client.rcs.test.updateAgent("agent_abc123def456", {
     *         displayName: "Acme Premium Support"
     *     })
     *
     * @example
     *     await client.rcs.test.updateAgent("agent_abc123def456", {
     *         displayName: "Acme Premium Support",
     *         description: "Premium support for Acme customers",
     *         color: "#0066FF"
     *     })
     */
    public updateAgent(
        agentId: string,
        request: Pinnacle.rcs.UpdateTestAgentRequest = {},
        requestOptions?: Test.RequestOptions,
    ): core.HttpResponsePromise<Pinnacle.TestAgentResponse> {
        return core.HttpResponsePromise.fromPromise(this.__updateAgent(agentId, request, requestOptions));
    }

    private async __updateAgent(
        agentId: string,
        request: Pinnacle.rcs.UpdateTestAgentRequest = {},
        requestOptions?: Test.RequestOptions,
    ): Promise<core.WithRawResponse<Pinnacle.TestAgentResponse>> {
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({ ...(await this._getCustomAuthorizationHeaders()) }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PinnacleEnvironment.Default,
                `rcs/test/agents/${core.url.encodePathParam(agentId)}`,
            ),
            method: "PATCH",
            headers: _headers,
            contentType: "application/json",
            queryParameters: requestOptions?.queryParams,
            requestType: "json",
            body: request,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Pinnacle.TestAgentResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Pinnacle.BadRequestError(_response.error.body as unknown, _response.rawResponse);
                case 401:
                    throw new Pinnacle.UnauthorizedError(
                        _response.error.body as Pinnacle.Error_,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Pinnacle.ForbiddenError(_response.error.body as Pinnacle.Error_, _response.rawResponse);
                case 404:
                    throw new Pinnacle.NotFoundError(_response.error.body as unknown, _response.rawResponse);
                case 500:
                    throw new Pinnacle.InternalServerError(
                        _response.error.body as Pinnacle.Error_,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PinnacleError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.PinnacleError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.PinnacleTimeoutError(
                    "Timeout exceeded when calling PATCH /rcs/test/agents/{agentId}.",
                );
            case "unknown":
                throw new errors.PinnacleError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    /**
     * Whitelist a phone number for testing with a specific test agent.
     *
     * During development and testing, RCS agents can only send messages to whitelisted phone numbers.
     * Use this endpoint to whitelist specific phone numbers so you can send and receive messages from the test agent.
     *
     * ## Verification Process
     *
     * After whitelisting, the recipient must accept the tester invite:
     *
     * 1. The recipient's device will receive a message from "RBM Tester Management".
     * 2. The recipient must tap "Make me a tester" to accept.
     * 3. Once accepted, the status transitions from `PENDING` to `ACCEPTED`.
     *
     * ## Verification
     *
     * <div style="display: flex; gap: 16px;">
     *   <div style="flex: 1; text-align: center;">
     *     <strong>Accepting the invite</strong><br/>
     *     <img src="https://pncl.to/f769cAvCbEx-MmezZjR6dz6KVkr5ZO" alt="Accepting the tester invite" style="max-width: 100%;" />
     *   </div>
     *   <div style="flex: 1; text-align: center;">
     *     <strong>Declining the invite</strong><br/>
     *     <img src="https://pncl.to/VRere3tEKfx4n0HNaxK-vwl7pbLHTJ" alt="Declining the tester invite" style="max-width: 100%;" />
     *   </div>
     * </div>
     *
     * ## Cooldown
     *
     * There is a **2-minute cooldown** after creating or updating a test agent before you can whitelist numbers.
     * Attempting to whitelist during the cooldown returns a `500` error with the remaining wait time.
     *
     * @param {string} agentId - The RCS agent ID (must be prefixed with `agent_`).
     * @param {Pinnacle.rcs.TestAgentWhitelistRequest} request
     * @param {Test.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Pinnacle.BadRequestError}
     * @throws {@link Pinnacle.UnauthorizedError}
     * @throws {@link Pinnacle.ForbiddenError}
     * @throws {@link Pinnacle.NotFoundError}
     * @throws {@link Pinnacle.InternalServerError}
     *
     * @example
     *     await client.rcs.test.whitelistNumber("agent_abc123def456", {
     *         phoneNumber: "+12345678901"
     *     })
     */
    public whitelistNumber(
        agentId: string,
        request: Pinnacle.rcs.TestAgentWhitelistRequest,
        requestOptions?: Test.RequestOptions,
    ): core.HttpResponsePromise<Pinnacle.TestAgentWhitelistResponse> {
        return core.HttpResponsePromise.fromPromise(this.__whitelistNumber(agentId, request, requestOptions));
    }

    private async __whitelistNumber(
        agentId: string,
        request: Pinnacle.rcs.TestAgentWhitelistRequest,
        requestOptions?: Test.RequestOptions,
    ): Promise<core.WithRawResponse<Pinnacle.TestAgentWhitelistResponse>> {
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({ ...(await this._getCustomAuthorizationHeaders()) }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PinnacleEnvironment.Default,
                `rcs/test/agents/${core.url.encodePathParam(agentId)}/whitelist`,
            ),
            method: "POST",
            headers: _headers,
            contentType: "application/json",
            queryParameters: requestOptions?.queryParams,
            requestType: "json",
            body: request,
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Pinnacle.TestAgentWhitelistResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Pinnacle.BadRequestError(_response.error.body as unknown, _response.rawResponse);
                case 401:
                    throw new Pinnacle.UnauthorizedError(
                        _response.error.body as Pinnacle.Error_,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Pinnacle.ForbiddenError(_response.error.body as Pinnacle.Error_, _response.rawResponse);
                case 404:
                    throw new Pinnacle.NotFoundError(_response.error.body as unknown, _response.rawResponse);
                case 500:
                    throw new Pinnacle.InternalServerError(
                        _response.error.body as Pinnacle.Error_,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PinnacleError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.PinnacleError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.PinnacleTimeoutError(
                    "Timeout exceeded when calling POST /rcs/test/agents/{agentId}/whitelist.",
                );
            case "unknown":
                throw new errors.PinnacleError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    /**
     * Check the current whitelist status of a phone number for a specific test agent.
     *
     * Use this endpoint to poll the status of a previously whitelisted number and determine
     * whether the recipient has accepted or rejected the tester invite.
     *
     * ## Status Values
     *
     * - **`PENDING`** — The tester invite was sent but the recipient has not yet responded. Ask the recipient to check their messages and accept the invite.
     * - **`ACCEPTED`** — The recipient accepted the invite. Messages can be exchanged.
     * - **`REJECTED`** — The recipient rejected the invite or the invite could not be delivered since the carrier does not support test agents. If the user rejected the invite, they can accept it again by tapping "Make me a tester" in the same message from "RBM Tester Management".
     *
     * <div style="display: flex; gap: 16px;">
     *   <div style="flex: 1; text-align: center;">
     *     <strong>Accepting the invite</strong><br/>
     *     <img src="https://pncl.to/f769cAvCbEx-MmezZjR6dz6KVkr5ZO" alt="Accepting the tester invite" style="max-width: 100%;" />
     *   </div>
     *   <div style="flex: 1; text-align: center;">
     *     <strong>Declining the invite</strong><br/>
     *     <img src="https://pncl.to/VRere3tEKfx4n0HNaxK-vwl7pbLHTJ" alt="Declining the tester invite" style="max-width: 100%;" />
     *   </div>
     * </div>
     *
     * @param {string} agentId - The RCS agent ID (must be prefixed with `agent_`).
     * @param {Pinnacle.rcs.TestGetWhitelistStatusRequest} request
     * @param {Test.RequestOptions} requestOptions - Request-specific configuration.
     *
     * @throws {@link Pinnacle.BadRequestError}
     * @throws {@link Pinnacle.UnauthorizedError}
     * @throws {@link Pinnacle.ForbiddenError}
     * @throws {@link Pinnacle.NotFoundError}
     * @throws {@link Pinnacle.InternalServerError}
     *
     * @example
     *     await client.rcs.test.getWhitelistStatus("agent_abc123def456", {
     *         phoneNumber: "+12345678901"
     *     })
     */
    public getWhitelistStatus(
        agentId: string,
        request: Pinnacle.rcs.TestGetWhitelistStatusRequest,
        requestOptions?: Test.RequestOptions,
    ): core.HttpResponsePromise<Pinnacle.TestAgentWhitelistResponse> {
        return core.HttpResponsePromise.fromPromise(this.__getWhitelistStatus(agentId, request, requestOptions));
    }

    private async __getWhitelistStatus(
        agentId: string,
        request: Pinnacle.rcs.TestGetWhitelistStatusRequest,
        requestOptions?: Test.RequestOptions,
    ): Promise<core.WithRawResponse<Pinnacle.TestAgentWhitelistResponse>> {
        const { phoneNumber } = request;
        const _queryParams: Record<string, string | string[] | object | object[] | null> = {};
        _queryParams.phoneNumber = phoneNumber;
        const _headers: core.Fetcher.Args["headers"] = mergeHeaders(
            this._options?.headers,
            mergeOnlyDefinedHeaders({ ...(await this._getCustomAuthorizationHeaders()) }),
            requestOptions?.headers,
        );
        const _response = await core.fetcher({
            url: core.url.join(
                (await core.Supplier.get(this._options.baseUrl)) ??
                    (await core.Supplier.get(this._options.environment)) ??
                    environments.PinnacleEnvironment.Default,
                `rcs/test/agents/${core.url.encodePathParam(agentId)}/numbers`,
            ),
            method: "GET",
            headers: _headers,
            queryParameters: { ..._queryParams, ...requestOptions?.queryParams },
            timeoutMs: (requestOptions?.timeoutInSeconds ?? this._options?.timeoutInSeconds ?? 60) * 1000,
            maxRetries: requestOptions?.maxRetries ?? this._options?.maxRetries,
            abortSignal: requestOptions?.abortSignal,
            fetchFn: this._options?.fetch,
            logging: this._options.logging,
        });
        if (_response.ok) {
            return { data: _response.body as Pinnacle.TestAgentWhitelistResponse, rawResponse: _response.rawResponse };
        }

        if (_response.error.reason === "status-code") {
            switch (_response.error.statusCode) {
                case 400:
                    throw new Pinnacle.BadRequestError(_response.error.body as unknown, _response.rawResponse);
                case 401:
                    throw new Pinnacle.UnauthorizedError(
                        _response.error.body as Pinnacle.Error_,
                        _response.rawResponse,
                    );
                case 403:
                    throw new Pinnacle.ForbiddenError(_response.error.body as Pinnacle.Error_, _response.rawResponse);
                case 404:
                    throw new Pinnacle.NotFoundError(_response.error.body as unknown, _response.rawResponse);
                case 500:
                    throw new Pinnacle.InternalServerError(
                        _response.error.body as Pinnacle.Error_,
                        _response.rawResponse,
                    );
                default:
                    throw new errors.PinnacleError({
                        statusCode: _response.error.statusCode,
                        body: _response.error.body,
                        rawResponse: _response.rawResponse,
                    });
            }
        }

        switch (_response.error.reason) {
            case "non-json":
                throw new errors.PinnacleError({
                    statusCode: _response.error.statusCode,
                    body: _response.error.rawBody,
                    rawResponse: _response.rawResponse,
                });
            case "timeout":
                throw new errors.PinnacleTimeoutError(
                    "Timeout exceeded when calling GET /rcs/test/agents/{agentId}/numbers.",
                );
            case "unknown":
                throw new errors.PinnacleError({
                    message: _response.error.errorMessage,
                    rawResponse: _response.rawResponse,
                });
        }
    }

    protected async _getCustomAuthorizationHeaders(): Promise<Record<string, string | undefined>> {
        const apiKeyValue = await core.Supplier.get(this._options.apiKey);
        return { "PINNACLE-API-KEY": apiKeyValue };
    }
}
