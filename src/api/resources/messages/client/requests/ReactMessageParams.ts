/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * @example
 *     {
 *         messageId: 1410,
 *         options: {
 *             force: true
 *         },
 *         reaction: "\uD83D\uDC4D"
 *     }
 */
export interface ReactMessageParams {
    /** Unique identifier of the message. */
    messageId: number;
    options?: ReactMessageParams.Options;
    /**
     * Unicode emoji to add. <br>
     *
     * Use `null` to remove existing reaction.
     */
    reaction?: string | null;
}

export namespace ReactMessageParams {
    export interface Options {
        /**
         * By default, only messages sent with the `options.tracking` field can be precisely reacted to. <br>
         *
         * Set `options.force` to true to bypass this restriction (may react to a different message). <br>
         *
         * For MMS and RCS, only messages containing text can be tracked. Otherwise, `options.force` is ignored.
         */
        force?: boolean;
    }
}
