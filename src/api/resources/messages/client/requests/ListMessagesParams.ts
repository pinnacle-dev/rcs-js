// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {}
 */
export interface ListMessagesParams {
    pageIndex?: number;
    pageSize?: number;
    direction?: ListMessagesParams.Direction;
    status?: ListMessagesParams.Status;
    type?: ListMessagesParams.Type;
    /** Filter by the method used to send the message. */
    method?: ListMessagesParams.Method;
    /** Filter by sender phone number (E.164 format) or agent id. */
    from?: string;
    /** Filter by recipient phone number (E.164 format). */
    to?: string;
    /** Search message content (partial match, case-insensitive). */
    content?: string;
    /** Filter messages created on or after this date (ISO 8601 format). */
    dateFrom?: string;
    /** Filter messages created on or before this date (ISO 8601 format). */
    dateTo?: string;
}

export namespace ListMessagesParams {
    export const Direction = {
        Inbound: "INBOUND",
        Outbound: "OUTBOUND",
    } as const;
    export type Direction = (typeof Direction)[keyof typeof Direction];
    export const Status = {
        Pending: "PENDING",
        Sent: "SENT",
        SendFailed: "SEND_FAILED",
        Delivered: "DELIVERED",
        DeliveryFailed: "DELIVERY_FAILED",
        Received: "RECEIVED",
        Read: "READ",
        Queued: "QUEUED",
        FallbackSent: "FALLBACK_SENT",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    export const Type = {
        Sms: "SMS",
        Mms: "MMS",
        Rcs: "RCS",
    } as const;
    export type Type = (typeof Type)[keyof typeof Type];
    /** Filter by the method used to send the message. */
    export const Method = {
        Api: "API",
        Sdk: "SDK",
        Other: "OTHER",
    } as const;
    export type Method = (typeof Method)[keyof typeof Method];
}
