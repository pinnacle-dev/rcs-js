// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {}
 */
export interface ListScheduledMessagesParams {
    pageIndex?: number;
    pageSize?: number;
    status?: ListScheduledMessagesParams.Status;
    /** Filter by schedule type — "MESSAGE" for individual messages, "BLAST" for bulk sends. */
    scheduleType?: ListScheduledMessagesParams.ScheduleType;
    /** Search scheduled message content (partial match, case-insensitive). */
    content?: string;
}

export namespace ListScheduledMessagesParams {
    export const Status = {
        Active: "ACTIVE",
        Cancelled: "CANCELLED",
        Failed: "FAILED",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];
    /** Filter by schedule type — "MESSAGE" for individual messages, "BLAST" for bulk sends. */
    export const ScheduleType = {
        Message: "MESSAGE",
        Blast: "BLAST",
    } as const;
    export type ScheduleType = (typeof ScheduleType)[keyof typeof ScheduleType];
}
