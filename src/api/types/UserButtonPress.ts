// This file was auto-generated by Fern from our API Definition.

import type * as Pinnacle from "../index.js";

/**
 * Discriminated union of button presses that can be simulated for a user.
 */
export type UserButtonPress =
    | Pinnacle.UserButtonPress.Trigger
    | Pinnacle.UserButtonPress.OpenUrl
    | Pinnacle.UserButtonPress.Call
    | Pinnacle.UserButtonPress.ScheduleEvent
    | Pinnacle.UserButtonPress.SendLocation
    | Pinnacle.UserButtonPress.RequestUserLocation;

export namespace UserButtonPress {
    export interface Trigger extends Pinnacle.UserButtonPressTrigger {
        type: "trigger";
    }

    export interface OpenUrl extends Pinnacle.UserButtonPressOpenUrl {
        type: "openUrl";
    }

    export interface Call extends Pinnacle.UserButtonPressCall {
        type: "call";
    }

    export interface ScheduleEvent extends Pinnacle.UserButtonPressScheduleEvent {
        type: "scheduleEvent";
    }

    export interface SendLocation extends Pinnacle.UserButtonPressSendLocation {
        type: "sendLocation";
    }

    export interface RequestUserLocation extends Pinnacle.UserButtonPressRequestUserLocation {
        type: "requestUserLocation";
    }
}
