/**
 * This file was auto-generated by Fern from our API Definition.
 */

export interface VcardAddress {
    /** Country name. */
    countryName?: string;
    /** Extended address information (apartment, suite, etc.). */
    extendedAddress?: string;
    /** Complete formatted address as a single string. */
    fulladdress?: string;
    /** City or locality. */
    locality?: string;
    /** ZIP code or postal code. */
    postalCode?: string;
    /** P.O. Box number. */
    postOfficeBox?: string;
    /** State, province, or region. */
    region?: string;
    /** Street address or street name and number. */
    streetAddress?: string;
    /** Address type classifications. */
    type?: VcardAddress.Type.Item[];
}

export namespace VcardAddress {
    export type Type = Type.Item[];

    export namespace Type {
        export type Item = "DOM" | "INTL" | "POSTAL" | "PARCEL" | "HOME" | "WORK" | "PREF";
        export const Item = {
            Dom: "DOM",
            Intl: "INTL",
            Postal: "POSTAL",
            Parcel: "PARCEL",
            Home: "HOME",
            Work: "WORK",
            Pref: "PREF",
        } as const;
    }
}
