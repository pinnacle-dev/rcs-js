/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Pinnacle from "../index.js";

export interface PhoneNumberCampaignAttach {
    /** List of phone numbers. */
    phoneNumbers: PhoneNumberCampaignAttach.PhoneNumbers.Item[];
    /** List of numbers that were not failed to be attached. */
    failed: PhoneNumberCampaignAttach.Failed.Item[];
}

export namespace PhoneNumberCampaignAttach {
    export type PhoneNumbers = PhoneNumbers.Item[];

    export namespace PhoneNumbers {
        export interface Item {
            /** Phone number (E.164 format). */
            phoneNumber: string;
            /** Campaign that the phone is now attached to. */
            campaign: Item.Campaign;
        }

        export namespace Item {
            /**
             * Campaign that the phone is now attached to.
             */
            export interface Campaign {
                /** Id of the campaign. */
                id: number;
                /** Campaign's name. */
                name: string;
                type: Pinnacle.ProfileType;
            }
        }
    }

    export type Failed = Failed.Item[];

    export namespace Failed {
        export interface Item {
            /** Phone number (E.164 format). */
            number: string;
            /** Error related to why the number was not attached. */
            error: string;
        }
    }
}
