// This file was auto-generated by Fern from our API Definition.

export interface AgentSummary {
    /** Unique identifier for the agent. */
    agentId: string;
    /** Type of the agent. */
    agentType: AgentSummary.AgentType;
    /** Service ID associated with the agent. */
    serviceId?: string | null;
    /** Per-carrier launch status for the agent. */
    carrierLaunches?: AgentSummary.CarrierLaunches | null;
    /** Agent configuration details. */
    config?: AgentSummary.Config | null;
    /** ISO 8601 timestamp of when the agent was created. */
    createdAt: string;
    /** ISO 8601 timestamp of when the agent was last updated. */
    updatedAt: string;
}

export namespace AgentSummary {
    /** Type of the agent. */
    export const AgentType = {
        Production: "PRODUCTION",
        Test: "TEST",
    } as const;
    export type AgentType = (typeof AgentType)[keyof typeof AgentType];

    /**
     * Per-carrier launch status for the agent.
     */
    export interface CarrierLaunches {
        /** Launch status for AT&T. null = not requested, false = pending, true = launched. */
        ATT?: boolean | null;
        /** Launch status for other carriers. null = not requested, false = pending, true = launched. */
        OTHERS?: boolean | null;
        /** Launch status for T-Mobile. null = not requested, false = pending, true = launched. */
        TMOBILE?: boolean | null;
        /** Launch status for Verizon. null = not requested, false = pending, true = launched. */
        VERIZON?: boolean | null;
    }

    /**
     * Agent configuration details.
     */
    export interface Config {
        /** Display name of the agent (max 40 characters). */
        name?: string;
        /** Description of the agent (max 100 characters). */
        description?: string;
        /** Hex color code for the agent. */
        color?: string;
        websites?: Config.Websites.Item[];
        phones?: Config.Phones.Item[];
        emails?: Config.Emails.Item[];
        /** Logo image URL. */
        iconUrl?: string | null;
        /** Hero image URL. */
        heroUrl?: string | null;
        /** Privacy policy URL. */
        privacyUrl?: string | null;
        /** Terms of service URL. */
        termsUrl?: string | null;
        /** Whether the agent supports two-way conversation. */
        isConversational?: boolean | null;
        /** Use case for the agent. */
        agentUseCase?: Config.AgentUseCase | null;
    }

    export namespace Config {
        export type Websites = Websites.Item[];

        export namespace Websites {
            export interface Item {
                url?: string;
                label?: string;
            }
        }

        export type Phones = Phones.Item[];

        export namespace Phones {
            export interface Item {
                phone?: string;
                label?: string;
            }
        }

        export type Emails = Emails.Item[];

        export namespace Emails {
            export interface Item {
                email?: string;
                label?: string;
            }
        }

        /** Use case for the agent. */
        export const AgentUseCase = {
            Transactional: "TRANSACTIONAL",
            Promotional: "PROMOTIONAL",
            Otp: "OTP",
            MultiUse: "MULTI_USE",
        } as const;
        export type AgentUseCase = (typeof AgentUseCase)[keyof typeof AgentUseCase];
    }
}
