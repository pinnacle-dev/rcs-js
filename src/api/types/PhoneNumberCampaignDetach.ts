/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Pinnacle from "../index.js";

export interface PhoneNumberCampaignDetach {
    /** List of phone numbers. */
    phoneNumbers: PhoneNumberCampaignDetach.PhoneNumbers.Item[];
    /** List of numbers that were not failed to be detached. */
    failed: PhoneNumberCampaignDetach.Failed.Item[];
}

export namespace PhoneNumberCampaignDetach {
    export type PhoneNumbers = PhoneNumbers.Item[];

    export namespace PhoneNumbers {
        export interface Item {
            /** Phone number (E.164 format). */
            phoneNumber: string;
            /** Campaign that the phone is now detached from. */
            campaign: Item.Campaign;
        }

        export namespace Item {
            /**
             * Campaign that the phone is now detached from.
             */
            export interface Campaign {
                /** Id of the campaign. */
                id: number;
                /** Campaign's name. */
                name: string;
                type: Pinnacle.ProfileType;
            }
        }
    }

    export type Failed = Failed.Item[];

    export namespace Failed {
        export interface Item {
            /** Phone number (E.164 format). */
            number: string;
            /** Error related to why the number was not detached. */
            error: string;
        }
    }
}
