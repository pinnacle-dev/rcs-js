/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Pinnacle from "../index.js";

export interface VCardData extends Pinnacle.VCardRes {
    /** File associated to the contact. */
    file: VCardData.File_;
}

export namespace VCardData {
    /**
     * File associated to the contact.
     */
    export interface File_ {
        /** Presigned URL to download the contact card. */
        url: string;
        /** Additional information about the download URL. */
        metadata: File_.Metadata;
    }

    export namespace File_ {
        /**
         * Additional information about the download URL.
         */
        export interface Metadata {
            /**
             * When the download URL expires in ISO 8601 format.<br>
             *
             * Null indicates that it is permanent.
             */
            expiresAt?: string | null;
        }
    }
}
