/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * Response containing all webhooks that match the requested identifiers.
 * Only includes the essential webhook information needed for identification and management.
 */
export interface WebhooksResponse {
    /** List of webhooks. */
    webhooks: WebhooksResponse.Webhooks.Item[];
}

export namespace WebhooksResponse {
    export type Webhooks = Webhooks.Item[];

    export namespace Webhooks {
        /**
         * Webhook configuration that defines where event notifications are sent.
         */
        export interface Item {
            /** URL where webhook events are delivered. */
            endpoint: string;
            /** Unique identifier for the webhook. */
            id: number;
            /** Name of the webhook. */
            name: string;
        }
    }
}
