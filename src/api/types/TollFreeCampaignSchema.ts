/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as Pinnacle from "../index.js";

export interface TollFreeCampaignSchema {
    /** Unique identifier for the campaign. */
    campaignId?: number;
    monthlyVolume?: Pinnacle.MessageVolume;
    /** Display name of the campaign. */
    name?: string;
    /** Opt-in keyword settings. */
    optIn?: TollFreeCampaignSchema.OptIn;
    /** Explain message that would be sent. */
    productionMessageContent?: string;
    /** Use case classification for the campaign. */
    useCase?: TollFreeCampaignSchema.UseCase;
}

export namespace TollFreeCampaignSchema {
    /**
     * Opt-in keyword settings.
     */
    export interface OptIn {
        method?: Pinnacle.OptInMethod;
        /** Url to opt in. */
        url?: string;
        /** Explain how users find your opt-in. */
        workflowDescription?: string;
    }

    /**
     * Use case classification for the campaign.
     */
    export interface UseCase {
        /** Summary of the use case. */
        summary?: string;
        value?: Pinnacle.TollFreeUseCases;
    }
}
